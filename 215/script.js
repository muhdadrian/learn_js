//the setTimeout or setInterval has two parameter: function (1st) and integer to store miliseconds (2nd). How ms to wait for setTimeout and how ms interval for setInterval
//the 1st parameter is function or callback until setTimeout we can call Higher Order Function
setTimeout(function () {
  console.log('Hello World');
}, 1000);

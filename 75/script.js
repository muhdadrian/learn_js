//Array manipulation

//4. To display array element use length method
var arr = ['Adrian', 'Hafizah', 'anakanak'];

for (var i = 0; i < arr.length; i++) {
  console.log('The student for ' + (i + 1) + ' : ' + arr[i]);
}

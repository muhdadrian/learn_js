//Example:

const button = document.querySelector('.submit');

button.addEventListener('click', function () {
  console.log('button clicked!');
});
//the click parameter above is event and the function parameter is callback. This is also higher order function

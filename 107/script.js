/*
DOM Selection Method:
1. getElementById(): 
- in a document there's only an element that can have a specific id - you can have a specific id for an element - if you have h1 with attribute id="title", you cannot have another element, which id is title - only an id in a page
- this is the simplest where we'll use this a lot
- RETURNED: element

2. getElementsByTagName():
- can have many 
- RETURNED: HTMLCollection

3. getElementsByClassName():
- class can be owned by more than an element
- RETURNED: HTMLCollection

4. querySelector()
- RETURNED: element

5. querySelectorAll()
- RETURNED: nodelist

# both methods of querySelector() and querySelectorAll are used to find element based on their selector
*/

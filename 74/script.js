//Array manipulation

//3. To delete array element
var arr = ['onemart', 'huphuat', 'savemore'];
arr[1] = undefined; // the element is not deleted, but the value changed to undefined
console.log(arr);

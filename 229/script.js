//Embedded Expression
console.log(`${1 + 1}`); //we can directly make expression. Expression could be as simple as addition in math

//we also can call function, as function is also an expression
console.log(`${alert('hello!')}`);

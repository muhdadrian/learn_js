//The code below is error. They are only for example

/*
let total = 0,
  count = 1;
while (count <= 10) {
  total += count;
  count += 1; //or count++
}
console.log(total);
*/

//Vs

//console.log(sum(range(1, 10))); //In the program there are function sum and range. When you call the two function simultaneously, there's function become a parameter for another function, it will be much simpler. It's only a line of code, as the difficulty we hide in the function
